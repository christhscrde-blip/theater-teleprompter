name: Final version 10 browser acceptance

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: final-v10-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  acceptance:
    if: github.event.pull_request.head.ref == 'hotfix/direct-json-v10'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Verify static runtime
        shell: bash
        run: |
          set -euo pipefail
          test -f theater-data.js
          test -f hotfix-v10.css
          test ! -f theater-loader.js
          grep -q 'theater-data.js?v=10' index.html
          grep -q 'presentationExitButton' index.html
          grep -q "presentationExitButton: \$('#presentationExitButton'\)" app.js
          grep -q 'hotfix-v10.css?v=10' service-worker.js
          node --check theater-data.js
          node --check app.js
          node --check service-worker.js

      - name: Install Chromium
        shell: bash
        run: |
          npm init -y >/dev/null 2>&1
          npm install --no-save playwright@1.55.0 >/dev/null
          npx playwright install --with-deps chromium >/dev/null

      - name: Start isolated server
        shell: bash
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 >/tmp/server.log 2>&1 &
          curl --retry 20 --retry-delay 1 --retry-connrefused -fsS http://127.0.0.1:4173/ >/dev/null

      - name: Run acceptance suite
        shell: bash
        run: |
          node <<'JS'
          const assert = require('node:assert/strict');
          const { chromium } = require('playwright');
          const URL = 'http://127.0.0.1:4173/?acceptance=v10';

          async function waitForApp(page) {
            await page.waitForFunction(() => {
              const state = window.__TELEPROMPTER__?.getState?.();
              return state?.version === 10 && state?.paragraphs === 1457 && state?.currentPage === 1;
            }, null, { timeout: 15000 });
          }

          async function openContext(browser, name, viewport) {
            const context = await browser.newContext({ viewport, serviceWorkers: 'allow' });
            await context.addInitScript(() => {
              try {
                Object.defineProperty(Document.prototype, 'fullscreenEnabled', {
                  configurable: true,
                  get: () => false,
                });
              } catch {}
            });
            const page = await context.newPage();
            const errors = [];
            page.on('pageerror', error => errors.push(error.message));
            page.on('console', message => {
              if (message.type() === 'error') errors.push(message.text());
            });
            const response = await page.goto(URL, { waitUntil: 'networkidle' });
            assert.equal(response.status(), 200, `${name}: HTTP`);
            await waitForApp(page);
            return { context, page, errors };
          }

          async function checkCore(page, name) {
            const state = await page.evaluate(() => window.__TELEPROMPTER__.getState());
            assert.deepEqual(
              {
                version: state.version,
                title: state.title,
                paragraphs: state.paragraphs,
                pages: state.pages,
                acts: state.acts,
                scenes: state.scenes,
                currentPage: state.currentPage,
              },
              {
                version: 10,
                title: 'Die Räuber',
                paragraphs: 1457,
                pages: 63,
                acts: 5,
                scenes: 15,
                currentPage: 1,
              },
              `${name}: state`,
            );
            assert.equal(await page.getByText('Fehler beim Laden').count(), 0, `${name}: loading error`);
            assert.equal(await page.locator('.script-line').count(), 1457, `${name}: rendered lines`);
            assert.equal(await page.locator('.script-line').first().textContent(), 'FRIEDRICH SCHILLER');
            assert.equal(await page.locator('.script-line').last().textContent(), 'ENDE');
          }

          async function checkPresentation(page, name) {
            await page.locator('#fullscreenButton').click();
            await page.waitForFunction(() => window.__TELEPROMPTER__.getState().presentation === true);
            await page.locator('#presentationExitButton').waitFor({ state: 'visible' });
            await page.locator('#presentationExitButton').click();
            await page.waitForFunction(() => window.__TELEPROMPTER__.getState().presentation === false);
            assert.equal(await page.locator('#presentationExitButton').isVisible(), false, `${name}: exit hides`);
          }

          async function setRange(page, selector, value) {
            await page.locator(selector).evaluate((element, next) => {
              element.value = String(next);
              element.dispatchEvent(new Event('input', { bubbles: true }));
            }, value);
          }

          (async () => {
            const browser = await chromium.launch({ headless: true });
            try {
              const desktop = await openContext(browser, 'desktop', { width: 1440, height: 900 });
              const { page, context, errors } = desktop;
              await checkCore(page, 'desktop');

              assert.equal(await page.locator('.nav-link').count(), 5);
              await page.locator('[data-nav="scenes"]').click();
              assert.equal(await page.locator('.nav-link').count(), 15);
              await page.locator('[data-nav="pages"]').click();
              assert.equal(await page.locator('.nav-link').count(), 64);

              await page.locator('#pageInput').fill('63');
              await page.locator('#pageJumpButton').click();
              await page.waitForFunction(() => window.__TELEPROMPTER__.getState().currentPage === 63);
              await page.locator('#previousPageButton').click();
              await page.waitForFunction(() => window.__TELEPROMPTER__.getState().currentPage === 62);
              await page.locator('#nextPageButton').click();
              await page.waitForFunction(() => window.__TELEPROMPTER__.getState().currentPage === 63);

              await page.evaluate(() => window.__TELEPROMPTER__.jumpToPage(1));
              await page.waitForTimeout(100);
              const before = await page.locator('#stage').evaluate(element => element.scrollTop);
              await page.locator('#playButton').click();
              await page.waitForTimeout(900);
              const after = await page.locator('#stage').evaluate(element => element.scrollTop);
              assert(after > before + 5, `desktop: auto-scroll ${before} -> ${after}`);
              await page.locator('#playButton').click();

              await setRange(page, '#speedInput', 50);
              await setRange(page, '#fontInput', 54);
              await setRange(page, '#lineHeightInput', 1.7);
              assert.equal(await page.locator('#speedValue').textContent(), '50');
              assert.equal(await page.locator('#fontValue').textContent(), '54');
              assert.equal(await page.locator('#lineHeightValue').textContent(), '1.70');

              await page.locator('#themeButton').click();
              assert.equal(await page.locator('body').evaluate(element => element.classList.contains('dark')), false);
              await page.locator('#themeButton').click();
              await checkPresentation(page, 'desktop');

              await page.keyboard.press('ArrowRight');
              await page.waitForFunction(() => window.__TELEPROMPTER__.getState().currentPage === 2);
              await page.keyboard.press('ArrowLeft');
              await page.waitForFunction(() => window.__TELEPROMPTER__.getState().currentPage === 1);
              await page.keyboard.press('Space');
              await page.waitForFunction(() => window.__TELEPROMPTER__.getState().isPlaying === true);
              await page.keyboard.press('Space');
              await page.waitForFunction(() => window.__TELEPROMPTER__.getState().isPlaying === false);

              await page.reload({ waitUntil: 'networkidle' });
              await waitForApp(page);
              assert.equal(await page.locator('#speedValue').textContent(), '50');
              assert.equal(await page.locator('#fontValue').textContent(), '54');

              await page.evaluate(() => navigator.serviceWorker.ready);
              await page.reload({ waitUntil: 'networkidle' });
              await page.waitForFunction(() => navigator.serviceWorker.controller !== null);
              await waitForApp(page);
              await context.setOffline(true);
              await page.reload({ waitUntil: 'domcontentloaded', timeout: 10000 });
              await waitForApp(page);
              await context.setOffline(false);
              assert.deepEqual(errors, [], 'desktop: browser errors');
              await context.close();

              for (const [name, viewport] of [
                ['iPad', { width: 820, height: 1180 }],
                ['iPhone', { width: 390, height: 844 }],
              ]) {
                const device = await openContext(browser, name, viewport);
                await checkCore(device.page, name);
                await device.page.locator('#menuButton').click();
                assert(await device.page.locator('body').evaluate(element => element.classList.contains('menu-open')));
                await device.page.locator('#closeMenuButton').click();
                assert.equal(await device.page.locator('body').evaluate(element => element.classList.contains('menu-open')), false);
                await checkPresentation(device.page, name);
                assert.deepEqual(device.errors, [], `${name}: browser errors`);
                await device.context.close();
              }

              console.log('Version 10 browser acceptance passed on desktop, iPad and iPhone.');
            } finally {
              await browser.close();
            }
          })().catch(error => {
            console.error(error);
            process.exit(1);
          });
          JS

      - name: Show server log on failure
        if: failure()
        run: cat /tmp/server.log || true
